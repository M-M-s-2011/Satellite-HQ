// this `index.js` is a great place to export everything
// when searching, the first file its found is always index.js
// so this allows every component to be found here
// rather than having to go to each component file separately

export { Game } from "./Game";
export { ChatLobby } from "./ChatLobby";
export { Home } from "./Home";
export { Space } from "./Space";
